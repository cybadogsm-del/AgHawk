import streamlit as st
import pandas as pd

# Setting layout to centered works best for mobile viewing
st.set_page_config(page_title="AgHawk", page_icon="🦅", layout="centered")

st.title("AgHawk 🦅")
st.write("App is live and ready for development.")
